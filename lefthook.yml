pre-commit:
  parallel: true
  jobs:
    - name: jsc
      run: pnpm rr jsc --fix-staged
      stage_fixed: true

    - name: tsc
      glob: "*.{ts,tsx,json}"
      run: pnpm rr tsc

pre-push:
  jobs:
    - name: test
      run: pnpm vitest run
